<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="create-table-notes" author="AD Coder">
        <comment>Create table notes</comment>
        <createTable tableName="notes">
            <column name="id" type="BIGINT" autoIncrement="true" generationType="ALWAYS">
                <constraints primaryKey="true" primaryKeyName="pk_notes" nullable="false"/>
            </column>

            <column name="title" type="VARCHAR(256)">
                <constraints nullable="false"/>
            </column>

            <column name="content" type="TEXT">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="${NOW}"/>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="${NOW}"/>
        </createTable>
    </changeSet>

</databaseChangeLog>