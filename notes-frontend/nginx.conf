server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location /api/notes/ {
        proxy_pass $API_BASE_URL;
        proxy_set_header Host $host;
    }
}
